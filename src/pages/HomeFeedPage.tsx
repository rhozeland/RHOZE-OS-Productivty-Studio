/**
 * HomeFeedPage — `/home`
 *
 * Home IS Flow. The swipeable feed renders inline here exactly like before —
 * no call-to-action card, no tiles. Users can still open `/flow` directly
 * for the dedicated fullscreen route.
 */

import FlowModePage from "@/pages/FlowModePage";

const HomeFeedPage = () => <FlowModePage />;

export default HomeFeedPage;
